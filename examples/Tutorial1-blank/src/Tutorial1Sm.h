// Autogenerated with StateSmith
#pragma once
#include <stdint.h>
// Some user .h file comment...

enum Tutorial1Sm_EventId
{
    Tutorial1Sm_EventId_DIM = 0,
    Tutorial1Sm_EventId_INCREASE = 1,
    Tutorial1Sm_EventId_OFF = 2,
};

enum
{
    Tutorial1Sm_EventIdCount = 3
};

enum Tutorial1Sm_StateId
{
    Tutorial1Sm_StateId_ROOT = 0,
    Tutorial1Sm_StateId_OFF = 1,
    Tutorial1Sm_StateId_ON1 = 2,
};

enum
{
    Tutorial1Sm_StateIdCount = 3
};

typedef struct Tutorial1Sm Tutorial1Sm;
typedef void (*Tutorial1Sm_Func)(Tutorial1Sm* sm);

struct Tutorial1Sm
{
    // Used internally by state machine. Feel free to inspect, but don't modify.
    enum Tutorial1Sm_StateId state_id;
    
    // Used internally by state machine. Don't modify.
    Tutorial1Sm_Func ancestor_event_handler;
    
    // Used internally by state machine. Don't modify.
    Tutorial1Sm_Func current_event_handlers[Tutorial1Sm_EventIdCount];
    
    // Used internally by state machine. Don't modify.
    Tutorial1Sm_Func current_state_exit_handler;
};

// State machine constructor. Must be called before start or dispatch event functions. Not thread safe.
void Tutorial1Sm_ctor(Tutorial1Sm* self);

// Starts the state machine. Must be called before dispatching events. Not thread safe.
void Tutorial1Sm_start(Tutorial1Sm* self);

// Dispatches an event to the state machine. Not thread safe.
void Tutorial1Sm_dispatch_event(Tutorial1Sm* self, enum Tutorial1Sm_EventId event_id);

// Converts a state id to a string. Thread safe.
const char* Tutorial1Sm_state_id_to_string(const enum Tutorial1Sm_StateId id);

