// Autogenerated with StateSmith
#include "Blinky1PrintfSm.h"
// this ends up in the generated c file
#include "app_timer.h"
#include "led.h"
#include <stdbool.h> // required for `consume_event` flag
#include <string.h> // for memset

static void ROOT_enter(Blinky1PrintfSm* self);
static void ROOT_exit(Blinky1PrintfSm* self);

static void LED_OFF_enter(Blinky1PrintfSm* self);
static void LED_OFF_exit(Blinky1PrintfSm* self);
static void LED_OFF_do(Blinky1PrintfSm* self);

static void LED_ON_enter(Blinky1PrintfSm* self);
static void LED_ON_exit(Blinky1PrintfSm* self);
static void LED_ON_do(Blinky1PrintfSm* self);

void Blinky1PrintfSm_ctor(Blinky1PrintfSm* self)
{
    memset(self, 0, sizeof(*self));
}

void Blinky1PrintfSm_start(Blinky1PrintfSm* self)
{
    ROOT_enter(self);
    // Transition to target state LED_OFF
    {
        // target state LED_OFF is a child of this state. No need to exit this state.
        
        // Enter towards target
        LED_OFF_enter(self);
        
        // update state_id
        self->state_id = Blinky1PrintfSm_StateId_LED_OFF;
    } // end of transition code
}

void Blinky1PrintfSm_dispatch_event(Blinky1PrintfSm* self, enum Blinky1PrintfSm_EventId event_id)
{
    Blinky1PrintfSm_Func behavior_func = self->current_event_handlers[event_id];
    
    while (behavior_func != NULL)
    {
        self->ancestor_event_handler = NULL;
        behavior_func(self);
        behavior_func = self->ancestor_event_handler;
    }
}

const char* Blinky1PrintfSm_state_id_to_string(const enum Blinky1PrintfSm_StateId id)
{
    switch (id)
    {
        case Blinky1PrintfSm_StateId_ROOT: return "ROOT";
        case Blinky1PrintfSm_StateId_LED_OFF: return "LED_OFF";
        case Blinky1PrintfSm_StateId_LED_ON: return "LED_ON";
        default: return "?";
    }
}

////////////////////////////////////////////////////////////////////////////////
// event handlers for state ROOT
////////////////////////////////////////////////////////////////////////////////

static void ROOT_enter(Blinky1PrintfSm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = ROOT_exit;
}

static void ROOT_exit(Blinky1PrintfSm* self)
{
    // State machine root is a special case. It cannot be exited.
    (void)self;  // nothing to see here compiler. move along!
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state LED_OFF
////////////////////////////////////////////////////////////////////////////////

static void LED_OFF_enter(Blinky1PrintfSm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = LED_OFF_exit;
    self->current_event_handlers[Blinky1PrintfSm_EventId_DO] = LED_OFF_do;
    
    // state behavior:
    {
        // uml action: turn_led_off();
        led_turn_off();;
    } // end of behavior code
    
    // state behavior:
    {
        // uml action: reset_timer();
        self->vars.timer_started_at_ms = app_timer_get_ms();
    } // end of behavior code
}

static void LED_OFF_exit(Blinky1PrintfSm* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[Blinky1PrintfSm_EventId_DO] = NULL;  // no ancestor listens to this event
}

static void LED_OFF_do(Blinky1PrintfSm* self)
{
    // setup handler for next ancestor that listens to `do` event
    self->ancestor_event_handler = NULL; // no ancestor state handles `do` event
    
    // state behavior:
    {
        // Note: no `consume_event` variable possible here because of state transition. The event must be consumed.
        // uml guard: after_ms(500)
        // uml transition target: LED_ON
        if (( (app_timer_get_ms() - self->vars.timer_started_at_ms) >= 500 ))
        {
            // Transition to target state LED_ON
            {
                // First, exit up to Least Common Ancestor Blinky1PrintfSm.
                while (self->current_state_exit_handler != ROOT_exit)
                {
                    self->current_state_exit_handler(self);
                }
                
                // Enter towards target
                LED_ON_enter(self);
                
                // update state_id
                self->state_id = Blinky1PrintfSm_StateId_LED_ON;
            } // end of transition code
            
            // Mark event as handled. Required because of transition.
            // self->ancestor_event_handler = NULL; // already done at top of function
            return; // event processing immediately stops when a transition occurs. No other behaviors for this state are checked.
        } // end of guard code
    } // end of behavior code
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state LED_ON
////////////////////////////////////////////////////////////////////////////////

static void LED_ON_enter(Blinky1PrintfSm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = LED_ON_exit;
    self->current_event_handlers[Blinky1PrintfSm_EventId_DO] = LED_ON_do;
    
    // state behavior:
    {
        // uml action: turn_led_on();
        //             reset_timer();
        led_turn_on();;
        self->vars.timer_started_at_ms = app_timer_get_ms();
    } // end of behavior code
}

static void LED_ON_exit(Blinky1PrintfSm* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[Blinky1PrintfSm_EventId_DO] = NULL;  // no ancestor listens to this event
}

static void LED_ON_do(Blinky1PrintfSm* self)
{
    // setup handler for next ancestor that listens to `do` event
    self->ancestor_event_handler = NULL; // no ancestor state handles `do` event
    
    // state behavior:
    {
        // Note: no `consume_event` variable possible here because of state transition. The event must be consumed.
        // uml guard: elapsed_ms > 1000
        // uml transition target: LED_OFF
        if ((app_timer_get_ms() - self->vars.timer_started_at_ms) > 1000)
        {
            // Transition to target state LED_OFF
            {
                // First, exit up to Least Common Ancestor Blinky1PrintfSm.
                while (self->current_state_exit_handler != ROOT_exit)
                {
                    self->current_state_exit_handler(self);
                }
                
                // Enter towards target
                LED_OFF_enter(self);
                
                // update state_id
                self->state_id = Blinky1PrintfSm_StateId_LED_OFF;
            } // end of transition code
            
            // Mark event as handled. Required because of transition.
            // self->ancestor_event_handler = NULL; // already done at top of function
            return; // event processing immediately stops when a transition occurs. No other behaviors for this state are checked.
        } // end of guard code
    } // end of behavior code
}


