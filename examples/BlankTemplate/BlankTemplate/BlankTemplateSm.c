// Autogenerated with StateSmith
#include "BlankTemplateSm.h"
            
#include <stdbool.h> // required for `consume_event` flag
#include <string.h> // for memset

static void ROOT_enter(BlankTemplateSm* self);
static void ROOT_exit(BlankTemplateSm* self);

static void STATE_1_enter(BlankTemplateSm* self);
static void STATE_1_exit(BlankTemplateSm* self);
static void STATE_1_do(BlankTemplateSm* self);

static void STATE_2_enter(BlankTemplateSm* self);
static void STATE_2_exit(BlankTemplateSm* self);
static void STATE_2_do(BlankTemplateSm* self);

void BlankTemplateSm_ctor(BlankTemplateSm* self)
{
    memset(self, 0, sizeof(*self));
}

void BlankTemplateSm_start(BlankTemplateSm* self)
{
    ROOT_enter(self);
    // Transition to target state STATE_1
    {
        // target state STATE_1 is a child of this state. No need to exit this state.
        
        // Enter towards target
        STATE_1_enter(self);
        
        // update state_id
        self->state_id = BlankTemplateSm_StateId_STATE_1;
    } // end of transition code
}

void BlankTemplateSm_dispatch_event(BlankTemplateSm* self, enum BlankTemplateSm_EventId event_id)
{
    BlankTemplateSm_Func behavior_func = self->current_event_handlers[event_id];
    
    while (behavior_func != NULL)
    {
        self->ancestor_event_handler = NULL;
        behavior_func(self);
        behavior_func = self->ancestor_event_handler;
    }
}

const char* BlankTemplateSm_state_id_to_string(const enum BlankTemplateSm_StateId id)
{
    switch (id)
    {
        case BlankTemplateSm_StateId_ROOT: return "ROOT";
        case BlankTemplateSm_StateId_STATE_1: return "STATE_1";
        case BlankTemplateSm_StateId_STATE_2: return "STATE_2";
        default: return "?";
    }
}

////////////////////////////////////////////////////////////////////////////////
// event handlers for state ROOT
////////////////////////////////////////////////////////////////////////////////

static void ROOT_enter(BlankTemplateSm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = ROOT_exit;
}

static void ROOT_exit(BlankTemplateSm* self)
{
    // State machine root is a special case. It cannot be exited.
    (void)self;  // nothing to see here compiler. move along!
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state STATE_1
////////////////////////////////////////////////////////////////////////////////

static void STATE_1_enter(BlankTemplateSm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = STATE_1_exit;
    self->current_event_handlers[BlankTemplateSm_EventId_DO] = STATE_1_do;
}

static void STATE_1_exit(BlankTemplateSm* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[BlankTemplateSm_EventId_DO] = NULL;  // no ancestor listens to this event
}

static void STATE_1_do(BlankTemplateSm* self)
{
    // setup handler for next ancestor that listens to `do` event
    self->ancestor_event_handler = NULL; // no ancestor state handles `do` event
    
    // state behavior:
    {
        // Note: no `consume_event` variable possible here because of state transition. The event must be consumed.
        // uml transition target: STATE_2
        // Transition to target state STATE_2
        {
            // First, exit up to Least Common Ancestor ROOT.
            while (self->current_state_exit_handler != ROOT_exit)
            {
                self->current_state_exit_handler(self);
            }
            
            // Enter towards target
            STATE_2_enter(self);
            
            // update state_id
            self->state_id = BlankTemplateSm_StateId_STATE_2;
        } // end of transition code
        
        // Mark event as handled. Required because of transition.
        // self->ancestor_event_handler = NULL; // already done at top of function
        return; // event processing immediately stops when a transition occurs. No other behaviors for this state are checked.
    } // end of behavior code
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state STATE_2
////////////////////////////////////////////////////////////////////////////////

static void STATE_2_enter(BlankTemplateSm* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = STATE_2_exit;
    self->current_event_handlers[BlankTemplateSm_EventId_DO] = STATE_2_do;
}

static void STATE_2_exit(BlankTemplateSm* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[BlankTemplateSm_EventId_DO] = NULL;  // no ancestor listens to this event
}

static void STATE_2_do(BlankTemplateSm* self)
{
    // setup handler for next ancestor that listens to `do` event
    self->ancestor_event_handler = NULL; // no ancestor state handles `do` event
    
    // state behavior:
    {
        // Note: no `consume_event` variable possible here because of state transition. The event must be consumed.
        // uml transition target: STATE_1
        // Transition to target state STATE_1
        {
            // First, exit up to Least Common Ancestor ROOT.
            while (self->current_state_exit_handler != ROOT_exit)
            {
                self->current_state_exit_handler(self);
            }
            
            // Enter towards target
            STATE_1_enter(self);
            
            // update state_id
            self->state_id = BlankTemplateSm_StateId_STATE_1;
        } // end of transition code
        
        // Mark event as handled. Required because of transition.
        // self->ancestor_event_handler = NULL; // already done at top of function
        return; // event processing immediately stops when a transition occurs. No other behaviors for this state are checked.
    } // end of behavior code
}


