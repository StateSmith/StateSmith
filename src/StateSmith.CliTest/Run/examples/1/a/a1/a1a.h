// Autogenerated with StateSmith 0.9.7-alpha.
// Algorithm: Balanced1. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

#pragma once
#include <stdint.h>

typedef enum a1a_EventId
{
    a1a_EventId_DO = 0, // The `do` event is special. State event handlers do not consume this event (ancestors all get it too) unless a transition occurs.
    a1a_EventId_MY_EVENT_1 = 1,
    a1a_EventId_MY_EVENT_2 = 2,
} a1a_EventId;

enum
{
    a1a_EventIdCount = 3
};

typedef enum a1a_StateId
{
    a1a_StateId_ROOT = 0,
    a1a_StateId_STATE_1 = 1,
    a1a_StateId_STATE_2 = 2,
} a1a_StateId;

enum
{
    a1a_StateIdCount = 3
};


// Generated state machine
// forward declaration
typedef struct a1a a1a;

// event handler type
typedef void (*a1a_Func)(a1a* sm);

// State machine constructor. Must be called before start or dispatch event functions. Not thread safe.
void a1a_ctor(a1a* sm);

// Starts the state machine. Must be called before dispatching events. Not thread safe.
void a1a_start(a1a* sm);

// Dispatches an event to the state machine. Not thread safe.
void a1a_dispatch_event(a1a* sm, a1a_EventId event_id);

// Thread safe.
char const * a1a_state_id_to_string(a1a_StateId id);

// Thread safe.
char const * a1a_event_id_to_string(a1a_EventId id);

// Generated state machine
struct a1a
{
    // Used internally by state machine. Feel free to inspect, but don't modify.
    a1a_StateId state_id;
    
    // Used internally by state machine. Don't modify.
    a1a_Func ancestor_event_handler;
    
    // Used internally by state machine. Don't modify.
    a1a_Func current_event_handlers[a1a_EventIdCount];
    
    // Used internally by state machine. Don't modify.
    a1a_Func current_state_exit_handler;
};

