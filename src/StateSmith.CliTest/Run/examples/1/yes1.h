// Autogenerated with StateSmith 0.9.9-alpha.
// Algorithm: Balanced1. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

#pragma once
#include <stdint.h>

typedef enum yes1_EventId
{
    yes1_EventId_DO = 0, // The `do` event is special. State event handlers do not consume this event (ancestors all get it too) unless a transition occurs.
    yes1_EventId_MY_EVENT_1 = 1,
    yes1_EventId_MY_EVENT_2 = 2,
} yes1_EventId;

enum
{
    yes1_EventIdCount = 3
};

typedef enum yes1_StateId
{
    yes1_StateId_ROOT = 0,
    yes1_StateId_STATE_1 = 1,
    yes1_StateId_STATE_2 = 2,
} yes1_StateId;

enum
{
    yes1_StateIdCount = 3
};


// Generated state machine
// forward declaration
typedef struct yes1 yes1;

// event handler type
typedef void (*yes1_Func)(yes1* sm);

// State machine constructor. Must be called before start or dispatch event functions. Not thread safe.
void yes1_ctor(yes1* sm);

// Starts the state machine. Must be called before dispatching events. Not thread safe.
void yes1_start(yes1* sm);

// Dispatches an event to the state machine. Not thread safe.
void yes1_dispatch_event(yes1* sm, yes1_EventId event_id);

// Thread safe.
char const * yes1_state_id_to_string(yes1_StateId id);

// Thread safe.
char const * yes1_event_id_to_string(yes1_EventId id);

// Generated state machine
struct yes1
{
    // Used internally by state machine. Feel free to inspect, but don't modify.
    yes1_StateId state_id;
    
    // Used internally by state machine. Don't modify.
    yes1_Func ancestor_event_handler;
    
    // Used internally by state machine. Don't modify.
    yes1_Func current_event_handlers[yes1_EventIdCount];
    
    // Used internally by state machine. Don't modify.
    yes1_Func current_state_exit_handler;
};

