Templates with a leading underscore are special (for test or other).

Note: dashes in directories are converted to underscores when stored as a C# manifest resource.
File name dashes are preserved however.
