// Autogenerated with StateSmith
#include "TestsMySm1.h"
#include <stdbool.h> // required for `consume_event` flag
#include <string.h> // for memset

// This function is used when StateSmith doesn't know what the active leaf state is at
// compile time due to sub states or when multiple states need to be exited.
static void exit_up_to_state_handler(TestsMySm1* self, TestsMySm1_Func desired_state_exit_handler);

static void ROOT_enter(TestsMySm1* self);

static void ROOT_exit(TestsMySm1* self);

static void S1_enter(TestsMySm1* self);

static void S1_exit(TestsMySm1* self);

static void S1_do(TestsMySm1* self);

static void S2_enter(TestsMySm1* self);

static void S2_exit(TestsMySm1* self);


// State machine constructor. Must be called before start or dispatch event functions. Not thread safe.
void TestsMySm1_ctor(TestsMySm1* self)
{
    memset(self, 0, sizeof(*self));
}

// Starts the state machine. Must be called before dispatching events. Not thread safe.
void TestsMySm1_start(TestsMySm1* self)
{
    ROOT_enter(self);
    // ROOT behavior
    // uml: TransitionTo(ROOT.InitialState)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ROOT.InitialState`.
        // ROOT.InitialState is a pseudo state and cannot have an `enter` trigger.
        
        // ROOT.InitialState behavior
        // uml: / { self->vars.b = false; } TransitionTo(S1)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: `self->vars.b = false;`.
            self->vars.b = false;
            
            // Step 3: Enter/move towards transition target `S1`.
            S1_enter(self);
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            self->state_id = TestsMySm1_StateId_S1;
            // No ancestor handles event. Can skip nulling `ancestor_event_handler`.
            return;
        } // end of behavior for ROOT.InitialState
    } // end of behavior for ROOT
}

// Dispatches an event to the state machine. Not thread safe.
void TestsMySm1_dispatch_event(TestsMySm1* self, TestsMySm1_EventId event_id)
{
    TestsMySm1_Func behavior_func = self->current_event_handlers[event_id];
    
    while (behavior_func != NULL)
    {
        self->ancestor_event_handler = NULL;
        behavior_func(self);
        behavior_func = self->ancestor_event_handler;
    }
}

// This function is used when StateSmith doesn't know what the active leaf state is at
// compile time due to sub states or when multiple states need to be exited.
static void exit_up_to_state_handler(TestsMySm1* self, TestsMySm1_Func desired_state_exit_handler)
{
    while (self->current_state_exit_handler != desired_state_exit_handler)
    {
        self->current_state_exit_handler(self);
    }
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state ROOT
////////////////////////////////////////////////////////////////////////////////

static void ROOT_enter(TestsMySm1* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = ROOT_exit;
}

static void ROOT_exit(TestsMySm1* self)
{
    // State machine root is a special case. It cannot be exited.
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state S1
////////////////////////////////////////////////////////////////////////////////

static void S1_enter(TestsMySm1* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = S1_exit;
    self->current_event_handlers[TestsMySm1_EventId_DO] = S1_do;
}

static void S1_exit(TestsMySm1* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[TestsMySm1_EventId_DO] = NULL;  // no ancestor listens to this event
}

static void S1_do(TestsMySm1* self)
{
    // No ancestor state handles `do` event.
    
    // S1 behavior
    // uml: do TransitionTo(S2)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        S1_exit(self);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `S2`.
        S2_enter(self);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        self->state_id = TestsMySm1_StateId_S2;
        // No ancestor handles event. Can skip nulling `ancestor_event_handler`.
        return;
    } // end of behavior for S1
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state S2
////////////////////////////////////////////////////////////////////////////////

static void S2_enter(TestsMySm1* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = S2_exit;
}

static void S2_exit(TestsMySm1* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
}
