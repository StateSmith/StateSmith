// Autogenerated with StateSmith.
// Algorithm: Balanced2. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

// Generated state machine
class RocketSm
{
fun userCodeMethod() {
    println('userCodeMethod');
}

    enum class EventId    {
        EV1, 
        EV2
    }
    
    val EventIdCount : Int = 2
    
    enum class StateId    {
        ROOT, 
        GROUP, 
        G1, 
        G2, 
        S1
    }
    
    val StateIdCount : Int = 5
    
    // Used internally by state machine. Feel free to inspect, but don't modify.
    var stateId: StateId  = StateId.ROOT
    
    // Starts the state machine. Must be called before dispatching events. Not thread safe.
    fun start()
    {
        ROOT_enter()
        // ROOT behavior
        // uml: TransitionTo(ROOT.<InitialState>)
        try {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `ROOT.<InitialState>`.
            // ROOT.<InitialState> is a pseudo state and cannot have an `enter` trigger.
            
            // ROOT.<InitialState> behavior
            // uml: TransitionTo(group)
            try {
                // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
                
                // Step 2: Transition action: ``.
                
                // Step 3: Enter/move towards transition target `group`.
                GROUP_enter()
                
                // group.<InitialState> behavior
                // uml: TransitionTo(g1)
                try {
                    // Step 1: Exit states until we reach `group` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
                    
                    // Step 2: Transition action: ``.
                    
                    // Step 3: Enter/move towards transition target `g1`.
                    G1_enter()
                    
                    // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
                    return
                } finally {} // end of behavior for group.<InitialState>
            } finally {} // end of behavior for ROOT.<InitialState>
        } finally {} // end of behavior for ROOT
    }
    
    // Dispatches an event to the state machine. Not thread safe.
    // Note! This function assumes that the `eventId` parameter is valid.
    fun dispatchEvent(eventId : EventId )
    {
        when (this.stateId)
        {
            // STATE: RocketSm
            StateId.ROOT -> 
{ } 
            
            // STATE: group
            StateId.GROUP -> 
                when (eventId)
                {
                    EventId.EV1 ->  GROUP_ev1() 
                else -> {}
                }

            
            // STATE: g1
            StateId.G1 -> 
                when (eventId)
                {
                    EventId.EV1 ->  G1_ev1() 
                else -> {}
                }

            
            // STATE: g2
            StateId.G2 -> 
                when (eventId)
                {
                    EventId.EV2 ->  G2_ev2() 
                    EventId.EV1 ->  GROUP_ev1()  // First ancestor handler for this event
                }

            
            // STATE: s1
            StateId.S1 -> 
{ } 
        }
    }
    
    // This function is used when StateSmith doesn't know what the active leaf state is at
    // compile time due to sub states or when multiple states need to be exited.
    private fun exitUpToStateHandler(desiredState : StateId )
    {
        while (this.stateId != desiredState)
        {
            when (this.stateId)
            {
                StateId.GROUP ->  GROUP_exit() 
                
                StateId.G1 ->  G1_exit() 
                
                StateId.G2 ->  G2_exit() 
                
                StateId.S1 ->  S1_exit() 
                
                else ->  return  // Just to be safe. Prevents infinite loop if state ID memory is somehow corrupted.
            }
        }
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ROOT
    ////////////////////////////////////////////////////////////////////////////////
    
    private fun ROOT_enter()
    {
        this.stateId = StateId.ROOT
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state GROUP
    ////////////////////////////////////////////////////////////////////////////////
    
    private fun GROUP_enter()
    {
        this.stateId = StateId.GROUP
    }
    
    private fun GROUP_exit()
    {
        this.stateId = StateId.ROOT
    }
    
    private fun GROUP_ev1()
    {
        // group behavior
        // uml: EV1 TransitionTo(s1)
        try {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            exitUpToStateHandler(StateId.ROOT)
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `s1`.
            S1_enter()
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            return
        } finally {} // end of behavior for group
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state G1
    ////////////////////////////////////////////////////////////////////////////////
    
    private fun G1_enter()
    {
        this.stateId = StateId.G1
    }
    
    private fun G1_exit()
    {
        this.stateId = StateId.GROUP
    }
    
    private fun G1_ev1()
    {
        var consume_event :         Boolean = false
        
        // g1 behavior
        // uml: EV1 [a > 20] TransitionTo(g2)
        if (a > 20)
        {
            // Step 1: Exit states until we reach `group` state (Least Common Ancestor for transition).
            G1_exit()
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `g2`.
            G2_enter()
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            return
        } // end of behavior for g1
        
        // Check if event has been consumed before calling ancestor handler.
        if (!consume_event)
        {
            GROUP_ev1()
        }
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state G2
    ////////////////////////////////////////////////////////////////////////////////
    
    private fun G2_enter()
    {
        this.stateId = StateId.G2
    }
    
    private fun G2_exit()
    {
        this.stateId = StateId.GROUP
    }
    
    private fun G2_ev2()
    {
        // g2 behavior
        // uml: EV2 TransitionTo(g1)
        try {
            // Step 1: Exit states until we reach `group` state (Least Common Ancestor for transition).
            G2_exit()
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `g1`.
            G1_enter()
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            return
        } finally {} // end of behavior for g2
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state S1
    ////////////////////////////////////////////////////////////////////////////////
    
    private fun S1_enter()
    {
        this.stateId = StateId.S1
    }
    
    private fun S1_exit()
    {
        this.stateId = StateId.ROOT
    }
companion object {
    
    // Thread safe.
    fun stateIdToString(id : StateId )
     : String     {
        when (id)
        {
            StateId.ROOT ->  return "ROOT"
            StateId.GROUP ->  return "GROUP"
            StateId.G1 ->  return "G1"
            StateId.G2 ->  return "G2"
            StateId.S1 ->  return "S1"
        }
     }
    
    // Thread safe.
    fun eventIdToString(id : EventId )
     : String     {
        when (id)
        {
            EventId.EV1 ->  return "EV1"
            EventId.EV2 ->  return "EV2"
        }
     }
}
}
