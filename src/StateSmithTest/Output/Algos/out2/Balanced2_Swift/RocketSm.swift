// Autogenerated with StateSmith.
// Algorithm: Balanced2. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

import Foundation
// Generated state machine
public class RocketSm
{
func userCodeMethod() {
    print("userCodeMethod")
}

    public enum EventId : Int
    {
        case EV1 = 0
        case EV2 = 1
    }
    
    public  let EventIdCount : Int = 2
    
    public enum StateId : Int
    {
        case ROOT = 0
        case GROUP = 1
        case G1 = 2
        case G2 = 3
        case S1 = 4
    }
    
    public  let StateIdCount : Int = 5
    
    // Used internally by state machine. Feel free to inspect, but don't modify.
    public var stateId: StateId  = .ROOT
    
    // State machine constructor. Must be called before start or dispatch event functions. Not thread safe.
    public init()
    {
    }
    
    // Starts the state machine. Must be called before dispatching events. Not thread safe.
    public func start()
    {
        self.ROOT_enter()
        // ROOT behavior
        // uml: TransitionTo(ROOT.<InitialState>)
        do {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `ROOT.<InitialState>`.
            // ROOT.<InitialState> is a pseudo state and cannot have an `enter` trigger.
            
            // ROOT.<InitialState> behavior
            // uml: TransitionTo(group)
            do {
                // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
                
                // Step 2: Transition action: ``.
                
                // Step 3: Enter/move towards transition target `group`.
                self.GROUP_enter()
                
                // group.<InitialState> behavior
                // uml: TransitionTo(g1)
                do {
                    // Step 1: Exit states until we reach `group` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
                    
                    // Step 2: Transition action: ``.
                    
                    // Step 3: Enter/move towards transition target `g1`.
                    self.G1_enter()
                    
                    // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
                    return
                } catch {} // end of behavior for group.<InitialState>
            } catch {} // end of behavior for ROOT.<InitialState>
        } catch {} // end of behavior for ROOT
    }
    
    // Dispatches an event to the state machine. Not thread safe.
    // Note! This function assumes that the `eventId` parameter is valid.
    public func dispatchEvent(_ eventId : EventId )
    {
        switch self.stateId
        {
            // STATE: RocketSm
            case StateId.ROOT:
                // No events handled by this state (or its ancestors).
                break

            
            // STATE: group
            case StateId.GROUP:
                switch eventId
                {
                    case EventId.EV1: self.GROUP_ev1() 
                    default: break
                }

            
            // STATE: g1
            case StateId.G1:
                switch eventId
                {
                    case EventId.EV1: self.G1_ev1() 
                    default: break
                }

            
            // STATE: g2
            case StateId.G2:
                switch eventId
                {
                    case EventId.EV2: self.G2_ev2() 
                    case EventId.EV1: self.GROUP_ev1()  // First ancestor handler for this event
                    default: break
                }

            
            // STATE: s1
            case StateId.S1:
                // No events handled by this state (or its ancestors).
                break

            default: break
        }
    }
    
    // This function is used when StateSmith doesn't know what the active leaf state is at
    // compile time due to sub states or when multiple states need to be exited.
    private func exitUpToStateHandler(_ desiredState : StateId )
    {
        while self.stateId != desiredState
        {
            switch self.stateId
            {
                case StateId.GROUP: self.GROUP_exit() 
                
                case StateId.G1: self.G1_exit() 
                
                case StateId.G2: self.G2_exit() 
                
                case StateId.S1: self.S1_exit() 
                
                default: return  // Just to be safe. Prevents infinite loop if state ID memory is somehow corrupted.
            }
        }
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ROOT
    ////////////////////////////////////////////////////////////////////////////////
    
    private func ROOT_enter()
    {
        self.stateId = StateId.ROOT
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state GROUP
    ////////////////////////////////////////////////////////////////////////////////
    
    private func GROUP_enter()
    {
        self.stateId = StateId.GROUP
    }
    
    private func GROUP_exit()
    {
        self.stateId = StateId.ROOT
    }
    
    private func GROUP_ev1()
    {
        // group behavior
        // uml: EV1 TransitionTo(s1)
        do {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            self.exitUpToStateHandler(StateId.ROOT)
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `s1`.
            self.S1_enter()
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            return
        } catch {} // end of behavior for group
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state G1
    ////////////////////////////////////////////////////////////////////////////////
    
    private func G1_enter()
    {
        self.stateId = StateId.G1
    }
    
    private func G1_exit()
    {
        self.stateId = StateId.GROUP
    }
    
    private func G1_ev1()
    {
        var consume_event :         Bool = false
        
        // g1 behavior
        // uml: EV1 [a > 20] TransitionTo(g2)
        if a > 20
        {
            // Step 1: Exit states until we reach `group` state (Least Common Ancestor for transition).
            self.G1_exit()
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `g2`.
            self.G2_enter()
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            return
        } // end of behavior for g1
        
        // Check if event has been consumed before calling ancestor handler.
        if !consume_event
        {
            self.GROUP_ev1()
        }
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state G2
    ////////////////////////////////////////////////////////////////////////////////
    
    private func G2_enter()
    {
        self.stateId = StateId.G2
    }
    
    private func G2_exit()
    {
        self.stateId = StateId.GROUP
    }
    
    private func G2_ev2()
    {
        // g2 behavior
        // uml: EV2 TransitionTo(g1)
        do {
            // Step 1: Exit states until we reach `group` state (Least Common Ancestor for transition).
            self.G2_exit()
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `g1`.
            self.G1_enter()
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            return
        } catch {} // end of behavior for g2
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state S1
    ////////////////////////////////////////////////////////////////////////////////
    
    private func S1_enter()
    {
        self.stateId = StateId.S1
    }
    
    private func S1_exit()
    {
        self.stateId = StateId.ROOT
    }
    
    // Thread safe.
    public static func stateIdToString(_ id : StateId )
     -> String     {
        switch id
        {
            case StateId.ROOT: return "ROOT"
            case StateId.GROUP: return "GROUP"
            case StateId.G1: return "G1"
            case StateId.G2: return "G2"
            case StateId.S1: return "S1"
            default: return "?"
        }
     }
    
    // Thread safe.
    public static func eventIdToString(_ id : EventId )
     -> String     {
        switch id
        {
            case EventId.EV1: return "EV1"
            case EventId.EV2: return "EV2"
            default: return "?"
        }
     }
}
