// Autogenerated with StateSmith
#include "Ex2.h"
#include <stdbool.h> // required for `consume_event` flag
#include <string.h> // for memset

static void ROOT_enter(Ex2* self);
static void ROOT_exit(Ex2* self);

static void STATE_1_enter(Ex2* self);
static void STATE_1_exit(Ex2* self);
static void STATE_1_do(Ex2* self);

static void STATE_2_enter(Ex2* self);
static void STATE_2_exit(Ex2* self);
static void STATE_2_ev2(Ex2* self);
static void STATE_2_myev1(Ex2* self);

// This function is used when StateSmith doesn't know what the active leaf state is at compile time due to sub states
// or when multiple states need to be exited.
static void exit_up_to_state_handler(Ex2* self, const Ex2_Func desired_state_exit_handler);


void Ex2_ctor(Ex2* self)
{
    memset(self, 0, sizeof(*self));
}

static void exit_up_to_state_handler(Ex2* self, const Ex2_Func desired_state_exit_handler)
{
    while (self->current_state_exit_handler != desired_state_exit_handler)
    {
        self->current_state_exit_handler(self);
    }
}

void Ex2_start(Ex2* self)
{
    ROOT_enter(self);
    // ROOT behavior
    // uml: TransitionTo(ROOT.InitialState)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `ROOT.InitialState`.
        // ROOT.InitialState is a pseudo state and cannot have an `enter` trigger.
        
        // ROOT.InitialState behavior
        // uml: TransitionTo(STATE_1)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `STATE_1`.
            STATE_1_enter(self);
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            self->state_id = Ex2_StateId_STATE_1;
            // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
            return;
        } // end of behavior for ROOT.InitialState
    } // end of behavior for ROOT
}

void Ex2_dispatch_event(Ex2* self, enum Ex2_EventId event_id)
{
    Ex2_Func behavior_func = self->current_event_handlers[event_id];
    
    while (behavior_func != NULL)
    {
        self->ancestor_event_handler = NULL;
        behavior_func(self);
        behavior_func = self->ancestor_event_handler;
    }
}

const char* Ex2_state_id_to_string(const enum Ex2_StateId id)
{
    switch (id)
    {
        case Ex2_StateId_ROOT: return "ROOT";
        case Ex2_StateId_STATE_1: return "STATE_1";
        case Ex2_StateId_STATE_2: return "STATE_2";
        default: return "?";
    }
}

////////////////////////////////////////////////////////////////////////////////
// event handlers for state ROOT
////////////////////////////////////////////////////////////////////////////////

static void ROOT_enter(Ex2* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = ROOT_exit;
}

static void ROOT_exit(Ex2* self)
{
    // State machine root is a special case. It cannot be exited.
    (void)self;  // nothing to see here compiler. move along!
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state STATE_1
////////////////////////////////////////////////////////////////////////////////

static void STATE_1_enter(Ex2* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = STATE_1_exit;
    self->current_event_handlers[Ex2_EventId_DO] = STATE_1_do;
}

static void STATE_1_exit(Ex2* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[Ex2_EventId_DO] = NULL;  // no ancestor listens to this event
}

static void STATE_1_do(Ex2* self)
{
    // No ancestor state handles `do` event.
    
    // STATE_1 behavior
    // uml: do TransitionTo(STATE_2)
    {
        // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
        STATE_1_exit(self);
        
        // Step 2: Transition action: ``.
        
        // Step 3: Enter/move towards transition target `STATE_2`.
        STATE_2_enter(self);
        
        // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
        self->state_id = Ex2_StateId_STATE_2;
        // No ancestor handles event. Can skip nulling `self->ancestor_event_handler`.
        return;
    } // end of behavior for STATE_1
}


////////////////////////////////////////////////////////////////////////////////
// event handlers for state STATE_2
////////////////////////////////////////////////////////////////////////////////

static void STATE_2_enter(Ex2* self)
{
    // setup trigger/event handlers
    self->current_state_exit_handler = STATE_2_exit;
    self->current_event_handlers[Ex2_EventId_EV2] = STATE_2_ev2;
    self->current_event_handlers[Ex2_EventId_MYEV1] = STATE_2_myev1;
}

static void STATE_2_exit(Ex2* self)
{
    // adjust function pointers for this state's exit
    self->current_state_exit_handler = ROOT_exit;
    self->current_event_handlers[Ex2_EventId_EV2] = NULL;  // no ancestor listens to this event
    self->current_event_handlers[Ex2_EventId_MYEV1] = NULL;  // no ancestor listens to this event
}

static void STATE_2_ev2(Ex2* self)
{
    // No ancestor state handles `ev2` event.
    
    // STATE_2 behavior
    // uml: EV2 / { stuff(); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = true; // events other than `do` are normally consumed by any event handler. Other event handlers in *this* state may still handle the event though.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `stuff();`
        stuff();
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for STATE_2
}

static void STATE_2_myev1(Ex2* self)
{
    // No ancestor state handles `myev1` event.
    
    // STATE_2 behavior
    // uml: myEv1 / { do_stuff(); }
    {
        // note: no ancestor consumes this event, but we output `bool consume_event` anyway because a user's design might rely on it.
        bool consume_event = true; // events other than `do` are normally consumed by any event handler. Other event handlers in *this* state may still handle the event though.
        (void)consume_event; // avoid un-used variable compiler warning. StateSmith cannot (yet) detect if behavior action code sets `consume_event`.
        
        // Step 1: execute action `do_stuff();`
        do_stuff();
        
        // Step 2: determine if ancestor gets to handle event next.
        // No ancestor handles event. Ignore `consume_event` flag.
    } // end of behavior for STATE_2
}


// Converts an event id to a string. Thread safe.
const char* Ex2_event_id_to_string(const enum Ex2_EventId id)
{
    switch (id)
    {
        case Ex2_EventId_DO: return "do";
        case Ex2_EventId_EV2: return "ev2";
        case Ex2_EventId_MYEV1: return "myev1";
    }
    return "?";
}
