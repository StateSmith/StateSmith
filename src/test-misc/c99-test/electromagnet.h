// Autogenerated with StateSmith 0.17.5+e9ecfe333d82335dd3f9a7f682f0bcaadb844e00.
// Algorithm: Balanced2. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

#pragma once  // You can also specify normal include guard. See https://github.com/StateSmith/StateSmith/blob/main/docs/settings.md
#include <stdint.h>

typedef enum electromagnet_EventId
{
    electromagnet_EventId_DO = 0, // The `do` event is special. State event handlers do not consume this event (ancestors all get it too) unless a transition occurs.
    electromagnet_EventId_ADC_CALIBRATION_COMPLETE = 1,
    electromagnet_EventId_CALIBRATION_FAILED = 2,
    electromagnet_EventId_CONTROL_ANOMALY = 3,
    electromagnet_EventId_GET_ON_STANDBY = 4,
    electromagnet_EventId_NEEDED_REFERRAL = 5,
    electromagnet_EventId_POWER_CONTROL = 6,
    electromagnet_EventId_STOP_THE_REQUEST = 7,
    electromagnet_EventId_TARGET_CURRENT_CHANGE = 8
} electromagnet_EventId;

enum
{
    electromagnet_EventIdCount = 9
};

typedef enum electromagnet_StateId
{
    electromagnet_StateId_ROOT = 0,
    electromagnet_StateId_ADC_CALIBRATION = 1,
    electromagnet_StateId_SYSTEM_ERROR = 2,
    electromagnet_StateId_SYSTEM_INITIALIZATION = 3,
    electromagnet_StateId_SYSTEM_RUN = 4,
    electromagnet_StateId_ELECTRICITY_CONTROL_MODE = 5,
    electromagnet_StateId_CHANGE_OF_OBJECTIVE = 6,
    electromagnet_StateId_ELECTRICITY_CONTROL = 7,
    electromagnet_StateId_INITIALIZATION_OF_PID = 8,
    electromagnet_StateId_PARAMETER_ADJUSTMENTS = 9,
    electromagnet_StateId_STANDBY = 10,
    electromagnet_StateId_SYSTEM_STOP = 11
} electromagnet_StateId;

enum
{
    electromagnet_StateIdCount = 12
};


// Generated state machine
// forward declaration
typedef struct electromagnet electromagnet;

// State machine constructor. Must be called before start or dispatch event functions. Not thread safe.
void electromagnet_ctor(electromagnet* sm);

// Starts the state machine. Must be called before dispatching events. Not thread safe.
void electromagnet_start(electromagnet* sm);

// Dispatches an event to the state machine. Not thread safe.
// Note! This function assumes that the `event_id` parameter is valid.
void electromagnet_dispatch_event(electromagnet* sm, electromagnet_EventId event_id);

// Thread safe.
char const * electromagnet_state_id_to_string(electromagnet_StateId id);

// Thread safe.
char const * electromagnet_event_id_to_string(electromagnet_EventId id);

// Generated state machine
struct electromagnet
{
    // Used internally by state machine. Feel free to inspect, but don't modify.
    electromagnet_StateId state_id;
};

